<%= form_with url: liked_path(@story.liked), method: :delete, remote: true do |f| %>
  <%= hidden_field_tag :user_id, current_user.id %>
  <%= hidden_field_tag :story_id, @story.id %>
  <%= f.submit t("dislike"), class: "btn btn-danger" %>
<% end %>
